[package]
name = "ethstore-cli"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]

[dependencies]
rustc-serialize = "0.3"
docopt = "0.7"
ethstore = { path = "../" }
ethcore-util = { path = "../../util" }

[[bin]]
name = "ethstore"
path = "src/main.rs"
doc = false
